# Core
PyYAML>=6.0

# Web dashboard
fastapi>=0.110
uvicorn[standard]>=0.27
jinja2>=3.1

# Resume parsing (optional but recommended)
pdfplumber>=0.10        # PDF resumes
python-docx>=1.1        # DOCX resumes

# Application automation (optional — install browsers with `playwright install`)
playwright>=1.42

# HTTP (for ollama / anthropic providers and real search adapters)
httpx>=0.27

# CLI niceties
rich>=13.7
